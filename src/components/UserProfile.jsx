import React from "react";

const UserProfile = () => (
  <div className="profile">
    <h2>User Profile</h2>
    <p>Connect with athletes, view notifications, and manage your profile.</p>
  </div>
);

export default UserProfile;
